-- table unique constraints --
ALTER TABLE REGION ADD CONSTRAINT UC_REGION UNIQUE (COUNTRY_ID,CODE) USING INDEX (CREATE INDEX IDX_REGION_UC ON REGION(COUNTRY_ID,CODE))
/

-- table foreign keys --
ALTER TABLE REGION ADD CONSTRAINT FK_REGION_0 FOREIGN KEY (COUNTRY_ID) REFERENCES COUNTRY
/

-- table indexes --
CREATE INDEX IDX_REGION_C0 ON REGION(COUNTRY_ID)
/

-- Specific Code Start --
-- Specific Code End --

